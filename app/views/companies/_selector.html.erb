<% unless current_user.companies.empty? %>
<li class="nav-item">
  <%= link_to company_dashboard_path(company_id: current_company.id), class: 'nav-link' do %>
    <i class="fa fa-building"></i> <%= current_company.name %>
  <% end %>
</li>
<% end %>