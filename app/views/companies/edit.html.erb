<div class="container mt-3">
  <div class="card">
    <div class="card-header">
      <h4 class="card-title"><%= t 'views.companies.edit.title' %></h4>
    </div>

    <div class="card-body">
      <%= render 'form' %>
    </div>
  </div>
</div>
