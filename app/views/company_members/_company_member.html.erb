<li class="list-group-item" id="company-member-<%= company_member.id %>">
  <%= company_member.user.name %>
  <%= link_to company_member_path(company_member), method: :delete, remote: true, class: 'btn btn-danger float-right' do %>
    <i class="fa fa-trash"></i>
  <% end %>
  <span class="badge badge-primary float-right">
    <%= t("activerecord.attributes.company_member.roles.#{company_member.role}") %>
  </span>
</li>