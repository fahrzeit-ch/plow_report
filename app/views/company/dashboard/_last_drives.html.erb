<div class="card">
  <div class="card-body">
    <%= link_to t('dashboard.cards.last_drives.show_all'), company_drives_path(current_company), class: 'float-right' %>
    <h5 class="card-title"><%= t 'dashboard.cards.last_drives.title' %></h5>
    <table class="table">
      <tbody>
      <% @last_drives.each do |drive| %>
        <%= render 'company/drives/drive_tr', drive: drive, tools: false %>
      <% end %>
      </tbody>
    </table>
    <% if @last_drives.empty? %>
      <div>
        <%= t 'views.common.info.no_data_available' %>
      </div>
    <% end %>
  </div>
</div>