<div class="card mt-3">
  <div class="card-header">
    <%= render 'navigation/company_edit_nav' %>
  </div>

  <div class="card-body">
    <h4 class="card-title"><%= link_to t('views.companies.activities.title'), company_activities_path(current_company) %> / <%= t'common.new' %></h4>
    <%= render 'form' %>
  </div>
</div>
