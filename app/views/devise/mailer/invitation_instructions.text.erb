<%= t("devise.mailer.invitation_instructions.hello", name: @resource.email) %>

<%= t("devise.mailer.invitation_instructions.someone_invited_you",
      url: root_url,
      invited_by: @resource.invited_by.name,
      role: CompanyMember.human_attribute_name(@resource.company_members.first.role)) %>

<%= accept_invitation_url(@resource, :invitation_token => @token) %>

<% if @resource.invitation_due_at %>
  <%= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %>
<% end %>

<%= strip_tags t("devise.mailer.invitation_instructions.ignore") %>
