# frozen_string_literal: true

{
    id: site.id,
    display_name: site.display_name,
    name: site.name,
    first_name: site.first_name,
    street: site.street,
    city: site.city,
    zip: site.zip,
    location: site.area_json,
    customer_id: site.customer_id,
    active: site.active,
    site_info: render("site_info", site_info: site.site_info),
    requires_value_for_ids: site.requires_value_for_ids,
    created_at: site.created_at,
    updated_at: site.updated_at
}
