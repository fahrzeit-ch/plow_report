<!DOCTYPE html>
<html lang="de">
  <%= render 'layouts/head' %>
  <body>
    <% if company_view? %>
      <%= render 'navigation/navbar_company_view' %>
    <% else %>
      <%= render 'navigation/navbar_main' %>
    <% end %>
    <div class="wrapper">

      <!-- Sidebar -->
      <nav id="sidebar" class="bg-light pt-3">
        <% if company_view? %>
          <%= render 'navigation/side_nav_company' %>
        <% else %>
          <%= render 'navigation/side_nav_driver' %>
        <% end %>
      </nav>

      <!-- Page Content -->
      <div id="content">
        <div class="container-fluid">
          <%= yield %>
        </div>
      </div>
    </div>
    <footer class="bg-light">
      <div class="container-fluid">
        Fahrzeit.ch by innodrive GmbH - Kontakt: <%= link_to "info@fahrzeit.ch", "mailto:info@fahrzeit.ch" %> <span class="pull-right"><%= link_to "Webseite", "https://www.fahrzeit.ch" %> - <%= link_to "Datenschutz Erklärung", "https://www.fahrzeit.ch/datenschutz"%></span>
      </div>
    </footer>
    <div class="modal" tabindex="-1" role="dialog" id="dynamicModal">
      <div class="modal-dialog" id="modal-content" role="document">

      </div>
    </div>
    <%= custom_bootstrap_flash %>
  </body>
</html>
